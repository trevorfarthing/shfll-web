import styled from '@emotion/styled';

const FormContainer = styled.div`
  padding: 10px;
  margin-top: 50px;
`;

// Note: export styled MUI components with the 'as' keyword to convert to its correct type so that Typescript knows

const S = {
  FormContainer,
};
export default S;
